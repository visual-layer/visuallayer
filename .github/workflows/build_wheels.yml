name: Build

on:
  push:
    branches:
      - master

jobs:
  build-and-upload:
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-20.04, ubuntu-22.04]
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: 3.9

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install setuptools wheel twine

      - name: Build wheels
        run: |
          python setup.py bdist_wheel --dist-dir dist/
          # Get the package name from setup.py
          PACKAGE_NAME=$(python setup.py --name)
          # Include the OS name and package name in the filename of the wheel
          mv dist/*.whl dist/${PACKAGE_NAME}-${{ matrix.os }}.whl

      - name: Publish package to TestPyPI
        uses: pypa/gh-action-pypi-publish@release/v1
        with:
          password: pypi-AgENdGVzdC5weXBpLm9yZwIkZTliOGEzYmMtZjBkZi00ZmMxLWI2MzEtZjkxMzcxZGJjNGQ4AAITWzEsWyJ2bC1kYXRhc2V0cyJdXQACLFsyLFsiY2E2NGMxMjQtMTgzNS00YjVlLTg5YmYtOGY0OTNjOTYyN2E5Il1dAAAGIJcUsbr3opBgMqpVFIwq4vMpsmccCopgpbZDNpGJiAm-# ${{ secrets.TEST_PYPI_API_TOKEN }}
          repository-url: https://test.pypi.org/legacy/
